name: Validate file size
on:
  push:
    branches:
      - main

jobs:
  validate-file-size:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check file size
        run: |
          for filename in artifacts/*.wasm; do
            if [ -n "$(find "$filename" -prune -size -10000000)"]; then
              error
            else
              printf '%s is less than 1MB'
            fi
          done
